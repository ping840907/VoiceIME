pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        // Local AAR directory — drop sherpa-onnx-*.aar into app/libs/
        flatDir { dirs 'app/libs' }
    }
}
rootProject.name = "ElderlyAssistant"
include ':app'
